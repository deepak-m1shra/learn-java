package com.x.vuinner.serialization;

public class Book/* implements Comparable<Object>*/{

	private String title;
	private String year;
	private String ISBN;

	public Book(String title, String year, String iSBN) {
		this.title = title;
		this.year = year;
		ISBN = iSBN;
	}

	@Override
	public String toString() {
		return "Book [title=" + title + ", year=" + year + ", ISBN=" + ISBN + "]";
	}

	@Override
	public int hashCode() {
		final int prime = 31;
		int result = 1;
		result = prime * result + ((ISBN == null) ? 0 : ISBN.hashCode());
		result = prime * result + ((title == null) ? 0 : title.hashCode());
		result = prime * result + ((year == null) ? 0 : year.hashCode());
		return result;
	}

	@Override
	public boolean equals(Object obj) {
		if (this == obj)
			return true;
		if (obj == null)
			return false;
		if (getClass() != obj.getClass())
			return false;
		Book other = (Book) obj;
		if (ISBN == null) {
			if (other.ISBN != null)
				return false;
		} else if (!ISBN.equals(other.ISBN))
			return false;
		if (title == null) {
			if (other.title != null)
				return false;
		} else if (!title.equals(other.title))
			return false;
		if (year == null) {
			if (other.year != null)
				return false;
		} else if (!year.equals(other.year))
			return false;
		return true;
	}

	public String getTitle() {
		return title;
	}

	public void setTitle(String title) {
		this.title = title;
	}

	public String getYear() {
		return year;
	}

	public void setYear(String year) {
		this.year = year;
	}

	public String getISBN() {
		return ISBN;
	}

	public void setISBN(String iSBN) {
		ISBN = iSBN;
	}

	/*@Override
	public int compareTo(Object o) {
		return (getTitle()+getYear()).compareTo(((Book)o).getTitle()+getYear());
		return 1;
	}*/

}
